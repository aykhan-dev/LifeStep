<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CircleSeekBar">
        <attr name="csb_progress" format="integer" />
        <attr name="csb_min" format="integer" />
        <attr name="csb_max" format="integer" />
        <attr name="csb_step" format="integer" />

        <attr name="csb_thumbDrawable" format="reference" />
        <attr name="csb_thumbSize" format="dimension" />

        <attr name="csb_progressColor" format="color" />
        <attr name="csb_progressWidth" format="dimension" />

        <attr name="csb_arcColor" format="color" />
        <attr name="csb_arcWidth" format="dimension" />

        <attr name="csb_textSize" format="dimension" />
        <attr name="csb_textColor" format="color" />

        <attr name="csb_isShowText" format="boolean" />

    </declare-styleable>

    <declare-styleable name="WeeklyBarDiagram">
        <attr name="singleBarLine" format="boolean" />
        <attr name="colorFirstPart" format="color" />
        <attr name="colorSecondPart" format="color" />
        <attr name="gap" format="dimension" />
        <attr name="barLineWidth" format="dimension" />
        <attr name="barLineCap" format="enum">
            <enum name="ROUND" value="0" />
            <enum name="BUTT" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="WalletCard">
        <attr name="image" format="reference" />
        <attr name="title" format="string" />
    </declare-styleable>

    <declare-styleable name="PinView">
        <!-- The number of digits in the pin -->
        <attr name="numDigits" format="integer" />
        <!-- Digit dimensions, default 50dp x 50dp -->
        <attr name="digitWidth" format="dimension" />
        <attr name="digitHeight" format="dimension" />
        <!-- Spacing between the pin boxes, default 20dp -->
        <attr name="digitSpacing" format="dimension" />
        <!-- Digit background, colour or drawable -->
        <attr name="digitBackground" format="reference|color" />
        <!-- Font size to use for the mask text, default 15sp -->
        <attr name="digitTextSize" format="dimension" />
        <!-- Colour of the mask text, defaults to theme textColorPrimary -->
        <attr name="digitTextColor" format="color" />
        <!-- Digit elevation (API 21+ only), default 0dp -->
        <attr name="digitElevation" format="dimension" />
        <!-- The character to use to mask the input -->
        <attr name="mask" format="string" />
        <!-- Accent color, defaults to theme colorAccent -->
        <attr name="pinAccentColor" format="color" />
        <!-- Accent width, default 3dp -->
        <attr name="accentWidth" format="dimension" />
        <!-- Accent type, default 'character' -->
        <attr name="accentType" format="enum">
            <enum name="none" value="0" />
            <enum name="all" value="1" />
            <enum name="character" value="2" />
        </attr>
        <!-- Input type -->
        <attr name="pinInputType" format="enum">
            <enum name="text" value="1" />
            <enum name="number" value="2" />
        </attr>
        <!-- Only draw accent when View has focus -->
        <attr name="accentRequiresFocus" format="boolean" />
    </declare-styleable>

    <declare-styleable name="DailyStatCard">
        <attr name="daily_stat_card_image" format="reference" />
        <attr name="daily_stat_card_count" format="string" />
        <attr name="daily_stat_card_title" format="string" />
    </declare-styleable>

    <declare-styleable name="CustomSwitch">
        <attr name="isSwitchedOn" format="boolean" />
        <attr name="unselectedThumbColor" format="reference" />
        <attr name="selectedThumbColor" format="reference" />
        <attr name="unselectedBackgroundColor" format="reference" />
        <attr name="android:selectedWeekBackgroundColor" format="reference" />
    </declare-styleable>

</resources>