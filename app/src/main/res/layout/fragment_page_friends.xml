<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.fragment.friends.page.PageFriendsFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:theme="@style/ScrollableTheme"
            android:id="@+id/recycler_view_friends"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="7.5dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:paddingStart="@dimen/page_padding_normal"
            android:paddingTop="12.5dp"
            android:paddingEnd="@dimen/page_padding_normal"
            android:paddingBottom="@dimen/page_padding_normal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_my_friends" />

    </FrameLayout>
</layout>